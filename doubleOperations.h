#pragma once
#include <math.h>


const double EPSILON = 1e-10;

// Compares two floating-point numbers and returns true if they are the same, otherwise returns false.
bool areSameDouble(double f, double s);
